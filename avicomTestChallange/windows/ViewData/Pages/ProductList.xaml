<Page x:Class="avicomTestChallange.windows.ViewData.Pages.ProductList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:avicomTestChallange.windows.ViewData.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductList" Loaded="Page_Loaded">

    <Grid Background="White">
        <DataGrid x:Name="grid" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding name}" Header="Наименование" Width="350"/>
                <DataGridTextColumn Binding="{Binding type}" Header="Тип лицензии" Width="150"/>
                <DataGridTextColumn Binding="{Binding price}" Header="Цена" Width="150"/>
                <DataGridTextColumn Binding="{Binding period}" Header="Период" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
